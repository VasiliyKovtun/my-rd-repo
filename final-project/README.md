# Playwright API & UI Tests

Проект для автоматизированного тестирования **API** и **UI** с использованием **Playwright**  
и генерацией отчётов **Allure single-file**.  
Тесты запускаются локально и в **GitHub Actions CI**.

## 🛠 Используемые технологии

- **Node.js**
- **TypeScript**
- **Playwright**
- **Allure Report (single-file)**
- **GitHub Actions (CI)**
- **GitHub Pages (публикация отчёта)**

---
##  Запуск проекта локально

### 1. Установить зависимости

cd final-project
npm install

### 2. Установить браузеры

npx playwright install

### 3. Создать файл .env на основе примера

.env.example

### 4. Заполнить логин и пароль

FOPHELP_LOGIN=your_login
FOPHELP_PASSWORD=your_password

### 5. Запуск только API тестов

npm run test:api

### 6. Запуск только API тестов с генерацией Allure отчета

npm run api:allure

### 7. Запуск только UI тестов 

npm run test:ui

### 8. Запуск только UI тестов с генерацией Allure отчета

npm run ui:allure

### 9. Открыть Allure отчета

npm run allure:open

### 10. CI Pipeline

Запуск при push и pull_request

Порядок выполнения:

    API tests

    UI tests (зависимы от API)

    Генерация Allure single-report

    Загрузка отчёта в Artifact и GitHub Pages

-------